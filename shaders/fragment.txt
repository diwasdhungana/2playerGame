#version 330 core

in vec3 fragmentColor;
in vec2 TexCoords;  // Change from fragmentTexCoords to TexCoords

out vec4 color;

uniform sampler2D imageTexture;

void main()
{
    color = texture(imageTexture, TexCoords);
    // color = vec4(fragmentColor, 1.0) * texture(imageTexture, TexCoords);
}
